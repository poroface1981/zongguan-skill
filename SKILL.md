---
name: zongguan
description: >
  总管 / 中控台 / 调度已安装技能。用户在对话里丢来项目或任务时使用：先读进化手册，给出最佳 skill 方案，等用户同意后再执行，做完复盘写入禁令/沿用/放大。Triggers on 总管, 中控台, 开始任务, 开始项目, 按方案执行, 复盘, dispatch skills, which skill.
metadata:
  short-description: "对话里的总管：先给方案，同意后执行，复盘进化"
user-invocable: true
---

# 总管 (zongguan)

对话调度器。**不是网站。** 用户只在聊天框里丢项目或任务。不要为此再做落地页、中控台 UI、仪表盘。

## 触发

- 用户说「总管」或丢来一个项目 / 任务 / 需求
- 用户说「按方案执行」
- 用户说「复盘」

## 每次任务的硬流程

1. **先读手册** `该 skill 目录下的 playbook/lessons.md`，在 Grok Build 里还读 `.grok/playbook/lessons.md`。禁令必须遵守。沿用默认带着。放大有余力再做。
2. **先给方案，不动手。** 用下面的固定格式，等用户同意。
3. **只有用户明确同意**（「做」「同意」「按方案执行」或等价）之后才执行。没同意，零文件改动。
4. **执行时**打开方案里主技能和搭配技能的 `SKILL.md`，按它们走，不换栈。
5. **做完复盘。** 提出禁令 / 沿用 / 放大各至多几条，等用户点头再写入 `playbook/lessons.md`。空话不入库。

## 方案格式（必须用这个，短）

```
方案
主技能：<folder>（一句话为什么）
搭配：<folder>、<folder>（没有就写「无」）
手册：命中的禁令/沿用，没有就写「无命中」
步骤：3-6 条，具体到打开哪份 SKILL.md
等你说「做」我再动手。
```

只推荐 **已经装好的 skill**。禁止发明不存在的 skill 名。

## 选技能的优先顺序

1. 手册禁令否决某条路，就换路。
2. 用户点名的 skill 优先。
3. 任务类型：
   - 现有站点改版 / 优化外观 / 不重做 → `redesign-skill` + `taste-skill` + `design-ui`
   - 落地页 / 品牌站 / 作品集 / 不那么模板 → `taste-skill` + `design-ui`
   - 图表 / 大屏 / 报表 / KPI → `smartcharts` + `design-ui`
   - 游戏 → `building-games` + `controls` + `design-ui`
   - 数据库 / 持久化 → `neon`
   - 登录 / 账号 → `auth`
   - 出图 / 视频 → `imagine`
   - 动效 / GSAP → `gsap-core` + `gsap-react`
   - 其余产品界面 → `design-ui`
4. 专项不够再加搭配。能少则少。

## 复盘写入格式

追加到 `playbook/lessons.md`：

```
## 禁令 | 标题
这次的事实，和下次如何杜绝。

## 沿用 | 标题
这次的事实，和下次如何带着。

## 放大 | 标题
这次的事实，和下次如何加强。
```

用户没确认的条目不准写。

## 禁止

- 为总管再做网站、控制台、仪表盘
- 没同意就改代码
- 口头说「我记住了」却不写手册
- 用未安装的 skill
